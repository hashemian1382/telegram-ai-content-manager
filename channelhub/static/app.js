let activeDraft=null;const $=s=>document.querySelector(s);const api=async(url,opts={})=>{const r=await fetch(url,{headers:{'Content-Type':'application/json'},...opts});const d=r.status===204?{}:await r.json();if(!r.ok)throw Error(d.error||'Request failed');return d};function toast(t){const e=$('#toast');e.textContent=t;e.classList.add('show');setTimeout(()=>e.classList.remove('show'),3200)}
function esc(s){const d=document.createElement('div');d.textContent=s;return d.innerHTML}async function load(){try{const [data,channels,drafts]=await Promise.all([api('/api/dashboard'),api('/api/channels'),api('/api/drafts')]);$('#stats').innerHTML=[['کانال منبع',data.channels],['پست‌های ذخیره‌شده',data.posts],['پیش‌نویس آماده',data.drafts],['منتشرشده',data.published]].map(x=>`<div class="stat"><b>${x[1]}</b><span>${x[0]}</span></div>`).join('');$('#channelCount').textContent=`${channels.length} کانال`;$('#channels').innerHTML=channels.length?channels.map(c=>`<div><a target="_blank" href="https://t.me/${c.username}">@${c.username}</a><button class="delete" onclick="removeChannel(${c.id})">حذف</button></div>`).join(''):'<p class="muted">هنوز کانالی اضافه نشده است.</p>';$('#drafts').innerHTML=drafts.length?drafts.map(d=>`<div><div><b>${d.status==='published'?'منتشر شد':'پیش‌نویس'}</b><br><small>${esc(d.text.slice(0,130))}${d.text.length>130?'…':''}</small></div><button class="secondary" onclick="openDraft(${d.id})">مشاهده</button></div>`).join(''):'<p class="muted">پیش‌نویسی وجود ندارد.</p>';window.drafts=drafts}catch(e){toast(e.message)}}
async function addChannel(e){e.preventDefault();try{await api('/api/channels',{method:'POST',body:JSON.stringify({username:$('#channel').value})});$('#channel').value='';toast('کانال اضافه شد.');load()}catch(e){toast(e.message)}}async function removeChannel(id){if(!confirm('کانال و پست‌های ذخیره‌شده‌اش حذف شوند؟'))return;try{await api('/api/channels/'+id,{method:'DELETE'});load()}catch(e){toast(e.message)}}async function scrape(){try{toast('دریافت پست‌ها شروع شد…');const d=await api('/api/scrape',{method:'POST'});toast(`${d.new} پست جدید ذخیره شد.`);load()}catch(e){toast(e.message)}}async function randomDraft(){try{activeDraft=await api('/api/drafts/random',{method:'POST'});renderComposer();load()}catch(e){toast(e.message)}}function openDraft(id){activeDraft=window.drafts.find(x=>x.id===id);renderComposer()}function renderComposer(){if(!activeDraft)return;const published=activeDraft.status==='published';$('#composer').className='composer';$('#composer').innerHTML=`${activeDraft.source_url?`<a class="muted" target="_blank" href="${activeDraft.source_url}">مشاهده منبع</a>`:''}<textarea id="draftText" ${published?'disabled':''}>${esc(activeDraft.text)}</textarea><div class="actions">${published?'<span class="status">این پست منتشر شده است</span>':`<button class="secondary" onclick="saveDraft()">ذخیره تغییرات</button><button class="primary" onclick="publishDraft()">انتشار در کانال</button>`}</div>`}async function saveDraft(){try{activeDraft=await api('/api/drafts/'+activeDraft.id,{method:'PATCH',body:JSON.stringify({text:$('#draftText').value})});toast('پیش‌نویس ذخیره شد.');load()}catch(e){toast(e.message)}}async function publishDraft(){if(!confirm('این متن اکنون در کانال منتشر شود؟'))return;try{await saveDraft();activeDraft=await api('/api/drafts/'+activeDraft.id+'/publish',{method:'POST'});renderComposer();toast('پست با موفقیت منتشر شد.');load()}catch(e){toast(e.message)}}load();
let sourcePosts=[];async function showMode(mode){const c=$('#creation');if(mode==='direct'){c.innerHTML=`<div class="ai-form"><label>متن پست</label><textarea id="directText" placeholder="متن پست خود را بنویسید…"></textarea><div class="create-actions"><button onclick="createDirect()">ساخت پیش‌نویس</button></div></div>`;return}if(!sourcePosts.length){try{sourcePosts=await api('/api/source-posts')}catch(e){toast(e.message)}}const models=['gemini-3.5-flash','gemini-3.5-flash-lite','gemini-3-flash','gemini-3.1-flash-lite','gemma-4-31b','gemma-4-26b'];c.innerHTML=`<div class="ai-form"><label>موضوع</label><input id="aiTopic" placeholder="مثلاً تازه‌ترین پژوهش‌ها درباره انرژی خورشیدی"><label>مدل</label><select id="aiModel">${models.map(m=>`<option>${m}</option>`).join('')}</select><label>لحن و طول</label><div style="display:flex;gap:8px"><select id="aiTone"><option value="professional">حرفه‌ای و علمی</option><option value="news">خبری و موجز</option><option value="friendly">صمیمی و آموزشی</option></select><select id="aiLength"><option value="short">کوتاه</option><option value="medium" selected>متوسط</option><option value="long">بلند</option></select></div><label>منابع اختیاری از پست‌های ذخیره‌شده</label><div class="source-picker">${sourcePosts.length?sourcePosts.map(p=>`<label><input type="checkbox" value="${p.id}"> @${esc(p.channel)} — ${esc(p.text.slice(0,100))}</label>`).join(''):'هنوز پستی در دیتابیس نیست.'}</div><div class="create-actions"><button onclick="generateAi()">تولید پیش‌نویس</button></div></div>`}async function createDirect(){try{activeDraft=await api('/api/drafts/direct',{method:'POST',body:JSON.stringify({text:$('#directText').value})});$('#creation').innerHTML='';renderComposer();toast('پیش‌نویس ساخته شد.');load()}catch(e){toast(e.message)}}async function generateAi(){try{const b=[...document.querySelectorAll('.source-picker input:checked')].map(x=>+x.value);toast('در حال تولید پیش‌نویس با Gemini…');activeDraft=await api('/api/drafts/generate',{method:'POST',body:JSON.stringify({topic:$('#aiTopic').value,model:$('#aiModel').value,tone:$('#aiTone').value,length:$('#aiLength').value,source_post_ids:b})});$('#creation').innerHTML='';renderComposer();toast('پیش‌نویس AI آماده شد.');load()}catch(e){toast(e.message)}}
